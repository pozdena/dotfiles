- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.config/nvim:
      path: config/nvim
    ~/.bashrc:
      path: bashrc
    ~/.bash_profile:
      path: bash_profile
