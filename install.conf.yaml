- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.bash:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.config/nvim:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.vim:
    ~/.vimrc:
