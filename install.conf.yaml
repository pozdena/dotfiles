- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.bash: bash
    ~/.bash_profile: bash_profile
    ~/.bashrc: bashrc
    ~/.config/nvim: config/nvim
    ~/.gitconfig: gitconfig
    ~/.gitignore_global: gitignore_global
